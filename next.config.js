/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/bininsight',
  images: { unoptimized: true },
};

module.exports = nextConfig;
